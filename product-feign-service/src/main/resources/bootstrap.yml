spring:
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled:  true
        serviceId:  product-config-service
eureka:
  client:
    serviceUrl:
      defaultZone:  http://localhost:8761/eureka/